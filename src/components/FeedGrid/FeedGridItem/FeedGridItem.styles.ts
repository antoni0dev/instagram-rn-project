import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {flex: 1, padding: 1, aspectRatio: 1, maxWidth: '33.33%'},
  image: {flex: 1},
  imageCollectionIcon: {position: 'absolute', right: 5, top: 5},
});

export default styles;
